package executor;

public enum CaseTwo {
	
	for (int i = 0; i<10; i++) {
		new Thread(() -> {
			System.out.println("Hello from" + Thread.currentThread().getName());
		}).start();
	}
	
	// 10 tane thread yaratma.

}
